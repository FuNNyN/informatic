#include <iostream>
using namespace std;
int main() {
    string message;
    int found,a,b ;
    setlocale(LC_ALL, "Russian");
    cout << "Введите слово : ";
    cin >> message;
    found = message.size();
    a = message[0];
    b = message[found - 1];
    message[0] = b;
    message[found - 1] = a;
    cout << message << endl;
}
