#include <iostream>
using namespace std;
int main() {
    int a;
    setlocale(LC_ALL, "Russian");
    cout << "Таблица умножения для числа : ";
    cin >> a;
    for (int i = 1; i < 11; i++) {
        cout << a << " * " << i << " = " << a * i << endl;
    }
}
